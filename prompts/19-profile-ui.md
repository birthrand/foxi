Read AGENTS.md first and follow it strictly.

Implement the Profile screen UI exactly as shown in the attached design with precise 8-point spacing throughout. Display the logged-in user information from Clerk and the selected language from Zustand + AsyncStorage.

Use the learning data from `data/*` to show lessons completed, progress, study time.

Use assets from the assets folder via the centralized images import.

@prompt_material/profile/profile-ui-screen.png
